import { DocumentSlot, UDocument } from '@/document'

export const slots: DocumentSlot[] = [
  {
    name: '-',
    description: '自定义列表内容'
  }
]

export const document: UDocument = { slots }

export default document
